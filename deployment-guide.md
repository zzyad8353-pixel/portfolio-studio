# دليل رفع PlayStation R2 مجانًا

## أولًا: ملف التسليم

بعد فك الضغط ستجد مشروعًا واحدًا يحتوي الموقعين معًا. لا ترفع `client` و`server` كموقعين منفصلين؛ لوحة الإدارة وواجهة العميل تشتركان في نفس الخادم وقاعدة البيانات.

- واجهة العميل: افتح الرابط الأساسي `/`.
- لوحة الإدارة: أضف `/admin` إلى نفس الرابط.
- قاعدة البيانات: الجداول معرفة في `drizzle/schema.ts` والترحيل موجود في `drizzle/0001_strange_wonder_man.sql`.
- العميل لا يرى رابط الإدارة؛ وعند فتح `/admin` يلزم إدخال كلمة السر المحلية `123456`.
- العميل ينشئ حسابًا برقم الهاتف وكلمة السر قبل تأكيد الحجز، ثم يجد حجوزاته في «حجوزاتي».

## الخيار الأسهل للمعاينة

استخدم رابط المعاينة الذي أرسلته لك في المحادثة. هذا الرابط يفتح النسخة العاملة من المسارين:

```text
https://3000-ie7m50c171yp0f6i86yz5-55488d69.sg2.manus.computer/
https://3000-ie7m50c171yp0f6i86yz5-55488d69.sg2.manus.computer/admin
```

الرابط مؤقت للمعاينة وليس دومينًا تجاريًا دائمًا.

## رفع مجاني مستمر للمشروع كاملًا

لأن المشروع Full-Stack ويحتاج Node.js وقاعدة بيانات، لا يكفي رفع ملفات `client` فقط على خدمة Static. استخدم خدمة استضافة تدعم Node.js للخادم، وخدمة قاعدة بيانات متوافقة مع MySQL/TiDB، ثم ضع متغيرات البيئة التالية في إعدادات الخدمة:

```text
DATABASE_URL=رابط قاعدة البيانات
JWT_SECRET=قيمة سرية طويلة
VITE_APP_ID=معرّف Manus OAuth
OAUTH_SERVER_URL=رابط خادم OAuth
VITE_OAUTH_PORTAL_URL=رابط بوابة تسجيل الدخول
OWNER_OPEN_ID=معرّف المالك
OWNER_NAME=اسم المالك
```

الخطوات العملية:

1. أنشئ Repository خاصًا أو عامًا على GitHub وارفع محتويات هذا المجلد.
2. أنشئ خدمة Web Service مجانية أو مجانية محدودة تدعم Node.js من مزود استضافة تختاره، واربطها بـ GitHub.
3. اجعل أمر البناء `pnpm build` وأمر التشغيل `pnpm start`.
4. أنشئ قاعدة بيانات MySQL/TiDB مجانية أو مجانية محدودة من مزود موثوق، ثم انسخ `DATABASE_URL` إلى إعدادات الخدمة.
5. نفّذ محتوى ملفي `drizzle/0001_strange_wonder_man.sql` و`drizzle/0002_wise_shatterstar.sql` مرة واحدة داخل قاعدة البيانات. إذا كانت جداول `users` غير موجودة، أنشئ جدول المستخدمين من `drizzle/schema.ts` أو استخدم ترحيل المشروع الأساسي.
6. أضف متغيرات البيئة السابقة، ثم أعد تشغيل الخدمة.
7. لا تحتاج لربط OAuth من أجل لوحة الإدارة؛ اختبر `/` و`/admin` ثم أدخل كلمة السر `123456`.

## خيار استضافة الواجهة فقط

إذا أردت معاينة Static بدون خادم وقاعدة بيانات، يمكن بناء الواجهة ورفع ناتج `dist/public` على خدمة Static مجانية. لكن في هذه الحالة لن تعمل الحجوزات الحقيقية ولا لوحة التحكم ولا الحفظ الدائم؛ لذلك لا تستخدم هذا الخيار إلا للعرض البصري.

## دخول لوحة الإدارة

افتح المسار `/admin` وأدخل كلمة السر المحلية الحالية:

```text
123456
```

لتغييرها قبل النشر العام، عدّل الثابت `ADMIN_PASSWORD` داخل `server/routers.ts` ثم أعد البناء والنشر.

## تنبيه أمان

تم تفعيل حماية `/admin` وتقييد عمليات الأجهزة والأسعار والحجوزات والإعدادات على جلسة المدير المحلية، بدون اعتماد على Manus OAuth. كلمات سر العملاء تُخزّن كـ hash مع salt. غيّر `ADMIN_PASSWORD` قبل النشر العام ولا تضع أي كلمات سر أو مفاتيح داخل ملفات المشروع، وتأكد من وضع `JWT_SECRET` قوي في بيئة الإنتاج.

## فحص ما قبل النشر

```bash
pnpm check
pnpm test
pnpm build
```

يجب أن تنجح الاختبارات الأربعة الموجودة حاليًا قبل رفع النسخة النهائية.
